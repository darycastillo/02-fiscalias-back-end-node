import dotenv from "dotenv";
dotenv.config({ path: ".env" });
/**
 * Todo:// si se necesita una configuracion adicional para apuntar a variables segun el ambiente
 * se tiene que hacer en este archivo :D
*/
